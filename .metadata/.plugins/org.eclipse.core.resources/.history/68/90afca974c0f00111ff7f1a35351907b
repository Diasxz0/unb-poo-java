package folha_Pagamento;

import java.util.Scanner;

public class Teste {

	public static void main(String[] args) {
		Scanner scan = new Scanner(System.in);
		
		String nome;
		double salario;
		int id;
		
		
		
		System.out.println("\n1 - Adicionar \n2 - Listar \n3- Sair");
		int escolha = scan.nextInt();
		scan.nextLine();
		boolean continuar = true;
		while(continuar) {
			switch(escolha) {
			case 1:
				
				System.out.print("Nome: ");
				nome = scan.nextLine();
				
				System.out.print("Salario: ");
				salario = scan.nextDouble();
				
				System.out.print("ID: ");
				id = scan.nextInt();
				scan.nextLine();
				
				System.out.println("\n1 - Desenvolvedor \n2 - Gerente \n3 - Estagiario");
				int escolha_2 = scan.nextInt();
				boolean continuar2 = true;
				while(continuar2) {
					switch(escolha_2) {
					case 1:
						
						System.out.print("linguagem: ");
						String linguagem = scan.nextLine();
						
						try {
							Desenvolvedor dev = new Desenvolvedor(nome, id, salario, linguagem);
						} catch (SalarioInvalido e){
							e.getMessage();
						} finally {
							scan.close();
							break;
						}
					case 2:
						System.out.print("Nome: ");
						String nome = scan.nextLine();
						
						System.out.print("Salario: ");
						double salario = scan.nextDouble();
						
						System.out.print("ID: ");
						int id = scan.nextInt();
						
						System.out.print("Bonificação Anual: ");
						double bonus = scan.nextDouble();
						
					}
				}
			}
		}
		
	}

}
